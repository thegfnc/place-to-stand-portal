{
  "name": "place-to-stand",
  "private": true,
  "packageManager": "npm@11.12.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "type-check": "turbo type-check",
    "cli:link": "npm run build -w @pts/cli && cd packages/cli && npm link"
  },
  "devDependencies": {
    "turbo": "^2.10.12",
    "typescript": "^6.0.3"
  }
}
